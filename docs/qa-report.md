# QA report — site v5.0 — Stage 3

Дата проверки: 2026-09-02
Baseline Этапа 3: commit `3390ba9d1d150d132cc81b77577f3f3cb4cd4240` (`site-v5: stage 2 qualifier flow`)

## 1. Изменённые файлы

- `index.html`;
- `assets/css/site.css`;
- `docs/content-blockers.md`;
- `docs/content-status.md`;
- `docs/qa-report.md`.

JavaScript, конфигурация, изображения и legacy-файлы в Этапе 3 не изменялись.

## 2. Реализованные требования

- сразу после `#top` создана одна секция `section#questions`;
- внутри секции добавлена широкая семантическая врезка `article` с реальным контекстом торговой компании;
- врезка выполнена как текстовый формат без вымышленного дашборда и без пустой рамки изображения;
- добавлена ссылка `Посмотреть устройство проекта →` с `href="#case-trade"`;
- до появления полного кейса на Этапе 5 добавлен технический целевой якорь `#case-trade`;
- добавлены четыре вопроса в порядке 01–04: деньги, прибыльность, решение, контроль;
- добавлена итоговая формула о порядке «вопрос → механизм → решение → автоматизация»;
- вся секция представлена статическим HTML и CSS и не зависит от JavaScript;
- навигационный offset вынесен в `--anchor-offset` и учитывает высоту фиксированной шапки плюс 20 px.

## 3. Контентная безопасность

B-007 не закрыт: первичный источник, даты, состав внедрения и разрешение на публикацию формулировки «два месяца» не предоставлены.

Поэтому:

- утверждение «система внедрена за два месяца» не опубликовано;
- сохранён подтверждённый в CURRENT торговый контекст и назначение связанной модели;
- видимая оговорка сообщает, что срок относится к одному конкретному проекту и будет указан только после проверки и разрешения;
- спорные проценты и другие клиентские показатели отсутствуют.

B-008 не блокирует Этап 3: утверждённого реального изображения нет, а ТЗ прямо разрешает текстовую врезку. Изображение и мокап не создавались.

## 4. Структурные проверки

| Проверка | Фактический результат | Статус |
|---|---|---|
| H1 | 1 | PASS |
| `#questions` | 1 | PASS |
| Вопросы | 4 | PASS |
| Порядок | `01, 02, 03, 04` | PASS |
| Ссылка на главный кейс | 1 × `href="#case-trade"` | PASS |
| Цель `#case-trade` | существует | PASS |
| Дублирующиеся ID | не найдены | PASS |
| Ссылки на отсутствующие hash-target | не найдены | PASS |
| Текст «два месяца» в `index.html` | отсутствует | PASS |
| Скрипты/изображения внутри `#questions` | отсутствуют | PASS |

Четыре вопроса и четыре пояснения попарно уникальны; одинаковый абзац в разных элементах не повторяется.

## 5. Проверенные viewport

| Размер | Композиция вопросов | Пересечение номера и текста | Горизонтальный overflow | Статус |
|---|---|---|---|---|
| 320 × 568 | одна колонка | нет, зазор 14 px | нет | PASS |
| 360 × 800 | одна колонка | нет | нет | PASS |
| 390 × 844 | одна колонка | нет | нет | PASS |
| 412 × 915 | одна колонка | нет | нет | PASS |
| 768 × 1024 | две колонки | нет | нет | PASS |
| 820 × 1180 | две колонки | нет | нет | PASS |
| 1024 × 768 | две колонки | нет | нет | PASS |
| 1280 × 720 | две колонки | нет | нет | PASS |
| 1366 × 768 | две колонки | нет | нет | PASS |
| 1440 × 900 | две колонки | нет | нет | PASS |
| 1920 × 1080 | две колонки | нет | нет | PASS |

На каждом размере `document.documentElement.scrollWidth` равен `clientWidth`; элементов `#questions`, выходящих за горизонтальные границы viewport, не найдено.

## 6. Sticky-header и ссылки

После завершения плавной прокрутки к `#questions` измерен фактический зазор между нижней границей фиксированной шапки и верхней границей секции:

| Размер | Низ шапки | Верх `#questions` | Зазор | Статус |
|---|---:|---:|---:|---|
| 320 × 568 | 65 px | 84 px | 19 px | PASS |
| 768 × 1024 | 65 px | 84 px | 19 px | PASS |
| 1366 × 768 | 69 px | 88 px | 19 px | PASS |

Активация ссылки на главный кейс установила `location.hash = "#case-trade"`; целевой ID существует. Полное содержимое кейса будет создано только на Этапе 5.

## 7. Доступность и читаемость

- секция имеет `aria-labelledby="questions-title"`;
- врезка размечена как `article` и связана со своим заголовком;
- вопросы оформлены упорядоченным списком `ol`;
- декоративные номера 01–04 скрыты от accessibility tree, порядок остаётся доступен через семантику списка;
- ссылка — нативный элемент `a` с реальным `href` и минимальной высотой 44 px; на 320 px измерено 48 px;
- содержание не скрывается классами `.js` и не требует выполнения скрипта;
- существующий глобальный `:focus-visible` сохранён;
- существующий `prefers-reduced-motion` сохраняет мгновенную прокрутку и отключает переходы.

Контраст ключевых пар по WCAG-формуле:

| Пара | Контраст | Статус для обычного текста |
|---|---:|---|
| `#765019` / `#eee5d5` | 5.73:1 | PASS |
| `#0d1a36` / `#eee5d5` | 13.79:1 | PASS |
| `#46516a` / `#eee5d5` | 6.35:1 | PASS |
| `#46516a` / `#fffaf1` | 7.63:1 | PASS |

## 8. Визуальная проверка

Сохранены и просмотрены итоговые снимки вне репозитория:

- `C:\Users\Ksenia\.codex\visualizations\2026\09\02\01a06133-5960-74f2-9167-f1952e127a6d\stage3\stage3-proof-1366x768.jpg`;
- `C:\Users\Ksenia\.codex\visualizations\2026\09\02\01a06133-5960-74f2-9167-f1952e127a6d\stage3\stage3-questions-1366x768.jpg`;
- `C:\Users\Ksenia\.codex\visualizations\2026\09\02\01a06133-5960-74f2-9167-f1952e127a6d\stage3\stage3-proof-320x568.jpg`;
- `C:\Users\Ksenia\.codex\visualizations\2026\09\02\01a06133-5960-74f2-9167-f1952e127a6d\stage3\stage3-questions-320x568.jpg`.

Проверено: редакционная, а не карточная композиция; врезка воспринимается как доказательство; текст не обрезан; номер и вопрос не сталкиваются; шапка не перекрывает начало блока.

## 9. Регрессионные и сетевые проверки

- `git diff --check`: PASS, только информационные предупреждения Git о будущем LF → CRLF;
- `node --check assets/js/site.js`: PASS;
- `node --check assets/js/analytics.js`: PASS;
- `node --check scripts/production-check.mjs`: PASS;
- HTML и семь локальных активов: 8 из 8 ответили HTTP 200;
- при 1366 × 768 обязательное содержимое hero заканчивается на 747 px, граница секции — на 767 px; требование первого экрана сохранено;
- внешние ресурсы в Этап 3 не добавлялись;
- `fetch`, `XMLHttpRequest`, `sendBeacon`, `localStorage`, cookies, inline handlers и `javascript:` URL не добавлялись;
- production-check с фактическим development-config завершился ожидаемым FAIL, exit code 1: окружение остаётся `development`, MAX и e-mail содержат placeholder-значения B-001/B-003.

## 10. Отклонения и ограничения

- Осознанное отклонение от буквального текста ТЗ: число «два месяца» исключено по более приоритетным `MASTER_CONTEXT.md`, `DECISIONS.md` и B-007. Это защита от публикации непроверенного клиентского факта.
- Реальное изображение не встроено по B-008; применён прямо разрешённый ТЗ текстовый вариант.
- `#case-trade` пока является техническим целевым якорем, так как полный кейс относится к Этапу 5.
- Отдельный запуск браузера с полностью отключённым JavaScript, screen reader и масштаб 200% не выполнялись. Независимость секции от JavaScript подтверждена структурой HTML/CSS и отсутствием JS-селекторов секции.
- Точные сообщения browser console в используемом канале проверки не выгружались; runtime-сбоев в выполненных сценариях не обнаружено.

## 11. Итог

Статус реализации Этапа 3: **PASS с документированным ограничением B-007**.

Статус числового доказательства «два месяца»: **BLOCKED до проверки и разрешения**.

Статус публикационной готовности сайта: **BLOCKED** по реальным контактам, юридическим данным и другим блокерам реестра.

Этап 4 не начат. Отчёт подготовлен для контрольного чернового коммита Этапа 3.
