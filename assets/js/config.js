// Insert only confirmed contact details before publication.
// MAX_URL is stored in maxUrl; CONTACT_EMAIL is stored in email.
// Empty values keep the visible placeholders and the hero link to #contact.
window.SITE_CONFIG = Object.freeze({
  maxUrl: '',
  email: '',
  legalName: 'ИП [полное имя]',
  inn: '[будет добавлен]',
  ogrnip: '[будет добавлен]',
  address: '[адрес]'
});
